{
  "name": "kaiserreich-atlas",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"pnpm graphql:dev\" \"pnpm web:dev\"",
    "graphql:dev": "pnpm -C packages/graphql start:dev",
    "web:dev": "pnpm -C packages/web dev"
  },
  "devDependencies": {
    "concurrently": "^6.5.0",
    "lerna": "^4.0.0"
  }
}
